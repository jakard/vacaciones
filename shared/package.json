{
  "name": "@vacaciones/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript types between web (Angular) and functions (Cloud Functions)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
